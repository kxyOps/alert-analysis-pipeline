# ── 知识库 KB 配置 ──
# kb.py / kb-server.py 共享的环境变量

# 数据目录（存放 fault-kb.json 和 fault-kb.db）
KB_DATA_DIR=./data

# 最大条目数（超出后 cleanup 会截断）
KB_MAX_ENTRIES=200

# 清理保留数（cleanup 时保留最近 N 条）
KB_CLEANUP_RETAIN=150

# Web UI 端口（仅 kb-server.py 使用）
KB_PORT=8888
