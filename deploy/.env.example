# ── n8n 环境变量 ──
# 复制为 deploy/.env，docker compose 自动加载

# Hermes webhook 地址
HERMES_WEBHOOK_URL=http://192.168.1.100:8644/webhooks/alert-receiver

# HMAC 签名密钥
WEBHOOK_SECRET=my-secret-key

# 监控的容器名（采集 Docker 日志用）
MONITOR_CONTAINER=my-app
